# 2026-08-22 16:18

- 为合并后的 touch-control Calcit 模块准备版本 `0.0.18`，同步 `package.json` 与 `deps.cirru`。
- CI 切换到 Node 24，显式安装 `calcit,caps`，增加 canonical Snapshot 格式与类型检查。
- 本地通过 caps、Calcit check-only、JS codegen、Yarn immutable 和 Vite production build。
