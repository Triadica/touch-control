# 2026-09-05 01:07 UTC Calcit 0.13.77 type migration

Agent / 身份: `codex-01a06771-818-touch-7c9e`

## English

- Aligned Calcit and `@calcit/procs` exactly at 0.13.77.
- Unwrapped indexed controller origins through explicit zero defaults before numeric subtraction.
- Narrowed the initial `performance.now` result to `Number` at the JS FFI boundary and removed two accidental single-expression call wrappers in the demo logger.
- Strengthened Linux validation with immutable Actions, Node 22 LTS, strict Caps/toolchain checks, format idempotency, check-only, attached tests, JavaScript generation, and the Vite build. The existing deployment section is unchanged.

## 中文

- 将 Calcit 与 `@calcit/procs` 精确对齐到 0.13.77。
- 在控制器坐标参与数值减法前显式解包索引结果，并使用零值默认项。
- 在 JS FFI 边界将初始 `performance.now` 结果收窄为 `Number`，并移除示例日志中的两个意外单表达式调用包装。
- 使用不可变 Actions、Node 22 LTS、strict Caps/toolchain、格式幂等、check-only、附着测试、JavaScript 生成与 Vite 构建强化 Linux 验证；现有部署段保持不变。
